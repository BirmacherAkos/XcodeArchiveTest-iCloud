# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'XcodeArchiveTest-iCloud' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XcodeArchiveTest-iCloud
  pod 'NewRelicAgent'
  target 'XcodeArchiveTest-iCloudTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'NewRelicAgent'
  end

  target 'XcodeArchiveTest-iCloudUITests' do
    inherit! :search_paths
    # Pods for testing
    pod 'NewRelicAgent'
  end

end

target 'XcodeArciteTest-NotificationServiceExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XcodeArciteTest-NotificationServiceExtension

end
